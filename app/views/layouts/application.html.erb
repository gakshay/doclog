<!DOCTYPE html>
<html>
<head>
  <title>eDakia</title>
  <link rel="shortcut icon" href="/images/favicon.ico" />
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  <div class="header" style="width: 900px; margin: auto;">
	<% if user_signed_in? %>  
		<%= link_to image_tag("logo.png"), "/transactions" %> 
	<% else %>  
		<%= link_to image_tag("logo.png"), "/" %> 
    <% end %>
	<div class="right">
	  <% if user_signed_in? %>  
		<%= current_user.mobile %> | <span><%= link_to "Sign out", destroy_user_session_path, :class => "button gray small" %></span>
		<span><%= link_to "Get Mail", receive_transactions_url, :class => "button gray small" %></span> 
		<span><%= link_to "Send Mail", new_transaction_url, :class => "button gray small" %></span>
	  <% else %>
		<span><%= link_to "Login", new_user_session_url, :class => "button gray small" %></span>  
		<span><%= link_to "Register", new_user_registration_url, :class => "button gray small" %></span> 
		<span><%= link_to "Get Mail", receive_transactions_url, :class => "button gray small" %></span> 
		<span><%= link_to "Send Mail", new_transaction_url, :class => "button gray small" %></span>
	  <% end %>
		<p class="notice"><%= notice %></p>
		<p class="alert"><%= alert %></p>
	</div>

  </div>
  <div class="content-wrapper">
	<div class="content">
		<%= yield %>
	</div>
  </div>
</body>
</html>
